{
  "name": "fatboard",
  "version": "0.1.0",
  "private": true,
  "description": "BREE stack project manager - Bun, React, Elysia, Eden",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "bun run --filter='*' dev",
    "dev:client": "bun run --filter=client dev",
    "dev:server": "bun run --filter=server dev",
    "build": "bun run --filter='*' build",
    "start": "bun run --filter=server start",
    "clean": "rm -rf node_modules client/node_modules server/node_modules client/dist server/dist"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
