# NATS 2.12 Configuration
port: 4222
monitor_port: 8222

jetstream {
  store_dir: "/nats_data"
  max_mem_store: 256MB
  max_file_store: 5GB
}

# Allow all connections for now (standard for this setup)
# In production, consider adding auth
